import { NextResponse } from 'next/server'

// TODO: Gemini function-calling endpoint — services/bot/run-bot-turn.ts (US-52..59)
export const POST = async () => {
  return NextResponse.json({ error: 'not implemented' }, { status: 501 })
}
